# Compact task output — 2026-09-17

Dropbox follow-up: the second user-provided saved page nests its heading inside `.f-title__body` and a `header.f-title`. Section discovery now traverses up to four heading-only wrappers, refusing to consume another section. A same-task `/dropbox` link labelled Upload Submission is recognized as an upload control; disabled classes/attributes are respected. The exact blank-state label No Dropbox Submissions produces `files: []` without exporting Google Drive popovers, account controls or decorative SVGs. An inconsistent empty label plus submission file rows fails explicitly. The actual saved page now returns the task description, three teacher-resource groups and `submission: {box: present, upload_control: enabled, files: []}`. 106 tests pass. This is saved-page verification, not a submission attempt or a fresh authenticated fetch.

The model-facing `get_task` response now uses `tools/task_output.py` after the existing one-page parser. It does not serialize the internal rich-content tree. Both MCP and the local inspector invoke the same formatter; developer `response.json` exports therefore match the new model payload.

- Instructions are faithful compact Markdown, not an AI summary. Blank layout paragraphs/groups disappear; code whitespace is preserved.
- Images, files, links and embeds remain in local `media` catalogues, referenced in content order. Relative/private source links are retained. They are references, not delivered binary content or proof of OCR.
- HTML tables remain structured rows/cells with captions and header/rowspan/colspan information, referenced from the text. Image-based tables stay images.
- Teacher attachments live in `teacher_resources`, separate from `submission.files`. Description attachments remain with the description's media. Submission timestamps/feedback remain when supplied in recognized content.
- Creation/history, discussions-not-requested, absent optional sections, internal assets/DOM trees and generic warning arrays are omitted from the model output. Missing optional fields mean not returned, not proven absent; the tool description states this once.
- `submission.box` is `present` for a recognized dropbox or file-upload form scoped to the requested task. Otherwise it is `not_detected`, not false/closed. `upload_control` is included only when a file input was found and reports enabled/disabled UI state, including disabled fieldsets. No submission is attempted, and an enabled input does not guarantee backend permission.
- Generic guide text, other-task forms and unrelated navigation do not establish submission availability. Other upload-widget variants still need real-page validation.

The task output schema and MCP instructions were updated. Existing saved responses are historical and are not overwritten. Approved and public deployments are unchanged.

Verification: 101 tests pass, including actual MCP payload framing, developer exports, observed saved-page layout, private attachment separation, empty/disabled upload controls, scoped upload forms, tables, Markdown emphasis and code whitespace. Replaying the attached real task HTML reduced serialized task bytes from 2325 to 722 (about 69%; not a token measurement). It retained the three instruction paragraphs and image reference. The saved page has no recognized submission UI, only submission-help text, so it returns not_detected. Browser-saved image paths remain offline references; live fetching is still needed to obtain a fresh online response.
